spring:
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/movies?useSSL=false
    username: moviefun
    password: moviefun
  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp
our.image.path: covers
blobStore: DBStore
